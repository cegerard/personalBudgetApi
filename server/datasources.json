{
  "pb-data": {
    "name": "pb-data",
    "connector": "mongodb",
    "host": "${MONGODB_HOST}",
    "port": "${MONGODB_PORT}",
    "database": "${MONGODB_NAME}",
    "username": "${MONGODB_USER}",
    "password": "${MONGODB_PSW}"
  }
}