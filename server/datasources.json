{
  "pb-data": {
    "host": "${MONGODB_HOST}",
    "port": "${MONGODB_PORT}",
    "database": "${MONGODB_NAME}",
    "username": "${MONGODB_USER}",
    "password": "${MONGODB_PSW}",
    "name": "pb-data",
    "connector": "mongodb"
  }
}
